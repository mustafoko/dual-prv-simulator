# Dual-Future PRV Simulator Dependencies
# Python 3.8+ required

# Core simulation uses only Python standard library:
# - random, statistics, dataclasses, enum, typing, argparse

# Optional: For plotting and visualization
matplotlib>=3.5.0
